zerops:
  - setup: docs0
    build:
      base: nodejs@22
      envVariables:
        CI: true
      buildCommands:
        - pnpm i
        - pnpm exec nx build docs-analog
      deployFiles:
        - dist/apps/docs-analog/client/~
      cache:
        - node_modules
    run:
      base: static
