export interface NitroExecutorSchema {
  preset: string;
  buildOutputPath: string;
  outputPath: string;
  staticBuildOutputPath: string;
  serverBuildOutputPath: string;
  runtimeRenderer: string;
} // eslint-disable-line
