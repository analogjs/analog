{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Nitro executor",
  "description": "",
  "type": "object",
  "properties": {
    "preset": {
      "type": "string",
      "description": "Build preset.",
      "default": "node-server"
    },
    "buildOutputPath": {
      "type": "string",
      "description": "Client build output directory."
    },
    "outputPath": {
      "type": "string",
      "description": "Build output directory."
    },
    "staticBuildOutputPath": {
      "type": "string",
      "description": "Build output directory."
    },
    "serverBuildOutputPath": {
      "type": "string",
      "description": "Build output directory."
    }
  },
  "required": [
    "buildOutputPath",
    "outputPath",
    "staticBuildOutputPath",
    "serverBuildOutputPath"
  ]
}
